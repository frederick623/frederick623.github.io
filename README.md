# Stock Dashboard

This dashboard is a static GitHub Pages site. It fetches public Yahoo Finance
market data directly in the browser, so it does not require a Flask server to
be running on the visitor's computer.

The legacy `app.py` backend remains available for local development, but it is
not used by the GitHub Pages deployment.