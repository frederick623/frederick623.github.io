# Stock Dashboard

The dashboard requires its Flask API; it cannot load market data when opened as
a standalone HTML file or through GitHub Pages.

```bash
pip install -r requirements.txt
python app.py
```

Open <http://localhost:5000> and search for a ticker such as `AAPL`.
